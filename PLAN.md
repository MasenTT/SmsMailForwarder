# 安卓短信转邮件应用：开发计划与进度清单

更新时间：2026-09-22

## 目标与交付

开发中文安卓应用：收到短信后，按短信正文正则匹配结果，转发到一个或多个邮箱。所有启用规则均参与匹配，合并收件人并去重；没有命中规则时才发送给默认收件人。

首版交付源码、可安装 APK、配置说明和测试报告。项目目录为 `/Users/onionjust/Downloads/SmsMailForwarder`。先完成 APK 和真机验证，再选择国内应用商店并处理上架。本文件为进度唯一清单，后续开发持续更新。

> 0.2.0 联系人管理、规则快速配置、v2→v3 迁移和 0.2.1 调试清单以 `/Users/onionjust/Downloads/SmsMailForwarder-PLAN.md` 为准；本文件随源码包保留同一项目的基础计划。

## 界面设计规范

- 风格：简洁、可靠的工具型设计，以 Material 3 为基础；中文界面，支持浅色和深色模式。
- 配色：蓝色主色、浅灰背景、白色卡片；绿色表示正常，橙色表示待处理，红色表示失败。状态必须配文字，不仅靠颜色区分。
- 布局：底部四个入口：首页、规则、记录、设置。适度圆角、清晰字号和留白，不使用复杂渐变或装饰动画。
- 首页：突出“短信转发已开启／已暂停”和总开关，展示今日转发数、待发送数、最近记录；配置未完成时展示设置引导及权限状态。
- 规则：卡片展示“匹配条件 → 收件邮箱”，支持直接启停；编辑页提供样例短信测试及命中结果。
- 记录：按时间排列，区分成功、等待、失败及结果不确定；正文默认折叠，详情展示逐收件人状态并允许重试。
- 设置：发件邮箱、默认收件人、运行权限、数据管理分组；SMTP 技术参数放在邮箱配置页。
- 可访问性：遵循 Material 触控尺寸，支持字体放大、读屏标签及足够对比度。每个页面覆盖空状态、加载、错误和正常状态。

## 功能与实现约定

- 技术：Kotlin、Jetpack Compose、Room、WorkManager；最低 Android 8.0，编译和目标版本采用开发时稳定版本。
- 首次配置完成后由用户主动开启。开启前必须具备短信权限、有效发件配置及至少一个默认收件人。
- 发件：首版一个发件账户，提供 QQ、163 配置入口以及自定义 SMTP 地址、端口、TLS、授权码；支持发送测试邮件。TLS 校验证书，不接受明文降级。
- 规则：名称、正则、一个或多个收件人、启用开关；支持新增、编辑、删除、测试。正文采用包含匹配，整条匹配可用 `^` 和 `$`。使用 RE2/J，保存前校验并提示不支持的语法。
- 邮件：按收件人分别发送，避免泄露其他收件地址；主题包含号码和时间，正文包含完整短信、接收时间、来源号码及可获取的 SIM 信息。
- 接收：监听 `SMS_RECEIVED` 和可解析的 `DATA_SMS_RECEIVED` 新短信广播，合并长短信后持久化；不读取历史短信，不处理彩信或 RCS。范围包含系统允许获取的短信；SMS Retriever/OTP 等受系统保护的消息可能不会交付给本应用。
- 数据：发件配置、规则、短信事件、逐收件人投递任务。接收时固定规则结果和收件人，后续规则修改不改变已排队任务。无需服务器或对外 API。
- 可靠性：重复广播去重；断网保留任务；临时错误指数退避，最多自动尝试 5 次；认证或配置错误暂停重试并提示修正；只重试尚未成功的收件人。
- 状态：SMTP 接受仅代表已提交邮件服务器，不代表已到收件箱。提交后超时可能导致结果不确定，明确标记，不承诺绝对不重复或固定秒数送达。
- 暂停：关闭总开关停止新增转发任务并暂停未发送任务，正在提交的邮件可能完成；重新开启继续处理未完成任务。
- 数据保护：Android Keystore 保护授权码及正文加密密钥；敏感信息不写入调试日志或系统备份。完成记录默认保留 7 天，未完成任务不自动删除。

## 任务清单

状态规则：`[ ]` 未完成，`[x]` 已完成；进行中或受阻事项保留未勾选，并在备注中注明。只有实施和对应验证均完成才勾选。每完成一个任务立即更新本文件的勾选、证据或结果及变更记录；不得把计划确认当作代码实现完成。

### 1. 计划与界面设计

- [x] 确认短信正文分流、全规则命中去重、未命中发默认收件人。
- [x] 确认手机直连邮箱、QQ／163 首版验证、先 APK 后国内商店上架。
- [x] 将整体设计风格与四个主页面设计要求纳入计划。
- [x] 建立可持续更新的任务清单。
- [x] 制作四个主页面及规则编辑、邮件配置、记录详情的界面稿。（已用真实应用页面替代静态稿，docs/screenshots 含四页及编辑／详情截图。）
- [ ] 检查浅色／深色、字体放大、读屏及空／错误状态设计。（浅色、深色、1.3 倍字体、空状态、权限错误已检查；实际 TalkBack 读屏尚未验证）

### 2. 工程与基础设施

- [x] 检查 Android SDK、JDK、构建工具环境并建立独立项目。（JDK 21、Gradle 9.3.1 官方校验通过、SDK 37.0 已安装）
- [x] 建立 Compose 主题、四个导航入口与通用界面组件。（模拟器、界面测试或本机 SMTP 测试通过；外部邮箱另行验证）
- [x] 建立 Room 数据模型及配置存储。（源码核查及首轮 RepositoryTest 验证；真实邮箱另行验证）
- [x] 完成 Keystore 加密、备份排除和日志脱敏。（源码核查及首轮 RepositoryTest 验证；真实邮箱另行验证）

### 3. 邮箱与收件人

- [x] 实现 QQ、163 和自定义 SMTP 发件配置及输入校验。（模拟器、界面测试或本机 SMTP 测试通过；外部邮箱另行验证）
- [x] 实现单个／多个默认收件人管理。（模拟器、界面测试或本机 SMTP 测试通过；外部邮箱另行验证）
- [x] 实现 TLS 邮件发送及测试邮件功能。（模拟器、界面测试或本机 SMTP 测试通过；外部邮箱另行验证）
- [x] 实现配置错误、认证失败、连接失败的明确反馈。（模拟器、界面测试或本机 SMTP 测试通过；外部邮箱另行验证）

### 4. 正则分流

- [x] 实现规则增删改查和启停。（模拟器、界面测试或本机 SMTP 测试通过；外部邮箱另行验证）
- [x] 实现 RE2/J 校验及样例短信测试。（模拟器、界面测试或本机 SMTP 测试通过；外部邮箱另行验证）
- [x] 实现全部命中、邮箱去重及默认收件人回退。（源码核查及首轮 RepositoryTest 验证；真实邮箱另行验证）
- [x] 验证中文、多行、无匹配、非法表达式及不支持语法。（RoutingTest 通过）

### 5. 短信接收与后台发送

- [x] 实现短信授权引导、接收广播及权限状态展示。（Android 15 实际 SMS 广播及权限撤销提示验证通过。）
- [x] 实现长短信合并、可获取的 SIM 信息及重复广播去重。（371 字符长短信完整合并；模拟器订阅信息可见；去重集成测试通过。实体双卡仍待验证。）
- [x] 实现短信入库、规则结果快照和逐收件人任务。（源码核查及首轮 RepositoryTest 验证；真实邮箱另行验证）
- [x] 实现 WorkManager 发送队列、断网恢复和最多 5 次自动尝试。（断网零尝试，平台网络约束满足后真实 Worker 自动重试；五次上限由集成测试验证。）
- [x] 实现部分成功、结果不确定、手动重试和配置修正后的恢复。（源码核查及首轮 RepositoryTest 验证；真实邮箱另行验证）
- [x] 实现总开关暂停／恢复及重启后的任务恢复。（暂停状态下短信不入队；模拟器重启后旧任务保留并能接收新短信。）

### 6. 页面与记录管理

- [x] 完成首页状态、配置引导、统计和最近记录。（模拟器、界面测试或本机 SMTP 测试通过；外部邮箱另行验证）
- [x] 完成规则列表、编辑和测试界面。（模拟器、界面测试或本机 SMTP 测试通过；外部邮箱另行验证）
- [x] 完成记录列表、详情、逐收件人状态和重试入口。（模拟器、界面测试或本机 SMTP 测试通过；外部邮箱另行验证）
- [x] 完成设置页及运行权限指引。（模拟器、界面测试或本机 SMTP 测试通过；外部邮箱另行验证）
- [x] 实现完成记录 7 天清理及用户清空记录功能。（源码核查及首轮 RepositoryTest 验证；真实邮箱另行验证）
- [x] 检查所有可见控件的操作、导航、状态更新和错误恢复。（5 项完整 UI 测试、模拟器人工操作及系统设置跳转验证；实体设备和读屏另验）

### 7. 测试与验收

- [x] 执行规则、收件人去重、默认回退和投递状态单元测试。（14 项核心 JVM 测试通过，见 core/build/reports/tests/test）
- [x] 验证断网恢复、认证错误、部分失败及超时不确定情况。（本机 SMTP、集成测试及模拟器队列验证通过；不等同于实际邮箱服务验证。）
- [x] 验证权限拒绝／撤销、暂停／恢复、进程重启和设备重启。（Android 15 模拟器及集成测试通过，实体手机厂商策略另验。）
- [ ] 使用 QQ 及 163 实际账户验证邮件到达（需测试账户配置）。
- [ ] 记录实际测试手机品牌、型号和 Android 版本。（已确认小米 14，厂商系统版本 3.0.302.0；Android 大版本待确认。）
- [ ] 真机验证锁屏、省电模式、长短信、双卡及验证码获取能力。
- [x] 完成首轮代码自查、构建检查及问题修复。（已交付测试构建通过 APK 构建与 Lint；后续增强单独跟踪。）
- [x] 为小米真机定位增加脱敏广播元数据日志和诊断脚本采集。（0.2.1 APK 与 39 项自动化测试已重新验证；小米 14 真机证据仍待接入。）
- [x] 设置页增加“复制诊断信息”，将设备、权限、开关和最近广播元数据复制为脱敏报告，便于没有 ADB 时反馈真机证据。
- [x] 诊断报告补充 Android／系统构建／安全补丁、短信 AppOps、系统敏感短信能力、默认短信角色、安装来源和省电状态，用于定位“小米设置已允许但系统未交付广播”。
- [x] 增加 `DATA_SMS_RECEIVED` 数据短信广播入口及同样的脱敏解析诊断，覆盖部分通知／验证码使用的数据短信下发方式；是否由小米 HyperOS 实际交付仍需真机验收。
- [x] 补充自动化无障碍检查。（5 项完整 UI 流程启用检查并通过；实际 TalkBack 验收仍保留在设计检查未完成项。）
- [x] 保留覆盖率报告基线并明确当前口径。（`docs/coverage` 中的 HTML 为 0.1.1 基线；0.2.1 以 14 项核心、8 项 SMTP、17 项 Android 测试作为功能验证，未把历史覆盖率数字冒充当前源码覆盖率。）
- [x] 加固短信去重摘要的本机密钥保护，并验证重复广播及升级兼容。（Keystore HMAC-SHA256，数据库 v1→v2 迁移保留任务；失败测试修复后通过。）
- [x] 汇总已通过、未通过及未验证项，明确实际兼容范围。（docs/TEST-REPORT.md 已更新）

### 8. 首版交付

- [x] 生成并验证可安装 APK，记录版本和签名方式。（模拟器、界面测试或本机 SMTP 测试通过；外部邮箱另行验证）
- [x] 整理源码、配置说明、权限及后台运行指引。（模拟器、界面测试或本机 SMTP 测试通过；外部邮箱另行验证）
- [x] 整理首轮测试报告、已知限制和文件路径。（报告、说明和实际截图均位于项目 docs 目录）
- [x] 后续增强验证通过后重新生成 APK、源码包、校验值和报告。（0.2.0 基线和 0.2.1 调试 APK、源码包、签名、哈希、安装冷启动和报告均已完成；小米 14、真实邮箱及 TalkBack 仍待验收。）

### 9. 后续上架（不计入首版 APK 完成条件）

- [ ] 确定具体国内应用商店。
- [ ] 核实目标商店对短信权限、应用用途及目标 Android 版本的要求。
- [ ] 根据渠道要求准备隐私政策、备案及其他审核材料。
- [ ] 准备正式发布签名、商店图片和应用说明。
- [ ] 提交审核并跟进结果（正式提交前取得用户明确授权）。

## 验收边界与待验证条件

- 已提供小米 14、系统版本 3.0.302.0；Android 大版本待确认。2026-09-17 用户截图显示，未知来源安装导致敏感短信权限被系统限制；解除及短信收发结果尚未验证。
- 验证码访问可能受新版安卓额外权限、SMS Retriever 标识和系统角色限制；以官方能力和真机结果为准，不绕过系统限制。
- 后台执行受系统及厂商省电策略约束；强行停止应用期间不保证接收，恢复行为必须实测并说明。
- 真实邮箱发送验证需要用户配置测试账户；授权码不得写入本计划、源码或报告。
- 首版不含服务器、注册登录、云同步、历史短信补转或商店审核通过承诺。

## 参考依据

- Android 短信广播：https://developer.android.com/reference/android/provider/Telephony.Sms.Intents
- Android 权限：https://developer.android.com/reference/android/Manifest.permission
- WorkManager：https://developer.android.com/develop/background-work/background-tasks/persistent

## 更新记录

| 日期 | 完成事项 | 验证或结果 |
| --- | --- | --- |
| 2026-09-16 | 整合原开发方案、界面风格和任务清单 | 计划文件已建立；尚未开始代码实现、界面稿制作或真机验证 |

### 当前实施状态（2026-09-22）

- 已完成独立 Android 工程、核心分流、加密存储、SMTP、后台队列和四个主页面；设计要求已纳入本计划。
- 当前自动化验证：14 项核心 JVM 测试、8 项本机 SMTP 测试、17 项 Android 集成／界面测试，共 39 项通过；Lint 0 错误。
- Android 15 模拟器已验证短信广播、371 字符长短信、断网队列、重启保留、暂停和权限撤销；浅色、深色与 1.3 倍字体已检查。
- 已完成无障碍自动检查、保留历史覆盖率基线和本机密钥去重摘要保护；新增摘要安全测试已由失败转为通过，数据库迁移已验证。
- 真机型号、QQ／163 实际投递、锁屏省电、双卡、验证码及 TalkBack 实测仍未完成。
- 当前代码为 0.2.1；目标 API 36，普通 APK、源码包、签名记录、构建日志和 SHA-256 已写入 Downloads。支持从旧版覆盖升级；APK 不含覆盖率插桩或测试证书。

### 清单维护约定

- 每完成一个任务，更新对应勾选、验证结果及本节记录。
- 进行中、失败或依赖真机／测试账户的任务保持未勾选，并写明原因。
- 发布包和当前源码存在差异时明确记录；只有重新构建、验证、打包后才更新交付状态。

| 日期 | 本次更新 | 状态 |
| --- | --- | --- |
| 2026-09-16 | 将整体 UI 风格、四页布局与交互要求纳入计划，建立分阶段清单 | 已完成 |
| 2026-09-16 | 更新首轮实现、29 项测试及模拟器验证证据 | 已完成 |
| 2026-09-16 | 增列无障碍、覆盖率及去重摘要加固任务，区分已验证测试包与后续源码增强 | 进行中 |

| 2026-09-16 | 完成 HMAC 去重、数据库无损迁移、自动无障碍及合并覆盖率验证，交付 0.1.1 测试包 | 已完成；真机与真实邮箱待验收 |

| 2026-09-17 | 记录小米 14、系统版本 3.0.302.0 及未知来源敏感权限拦截截图 | 真机验收进行中，待通过系统授权入口解除限制并复测 |
| 2026-09-22 | 增加联系人管理、联系人选择器、v2→v3 数据迁移及所有／验证码／金额／自定义快速规则；15 项 Android 测试通过 | 代码、自动化验证、普通 APK、源码包、签名、哈希和报告已完成；小米 14、真实邮箱及 TalkBack 仍待验收 |
| 2026-09-22 | 针对“权限已开但短信不转发”增加短信广播诊断、SMS_REJECTED 记录，将目标 API 调整为 36；16 项 Android 测试通过 | 0.2.1 调试 APK 已构建并在模拟器冷启动；小米 14 真机需根据诊断结果继续定位 |
| 2026-09-22 | 增加脱敏 `SmsMailForwarder` 广播元数据日志，诊断脚本采集该日志；重新构建 APK 并完成 39 项自动化测试和模拟器注入验证 | 0.2.1 诊断包已更新；小米 14 真机仍待接入 |
| 2026-09-22 | 根据真机反馈普通短信转发已恢复；补充 `DATA_SMS_RECEIVED` 监听与解析状态；重新完成 39 项自动化测试、17/17 Android 测试、Lint、APK 签名及源码包完整性校验 | 新 APK SHA-256 为 `ca8af043d73e631c23f045863239783998431ea3a94c999f07a692171c640c57`；源码包 SHA-256 为 `52df8291aab2b88eb47d9597073ad94edd19c2b10394180c6e6821739575d790`；通知／验证码是否能转发仍需单独确认 |
| 2026-09-22 | 诊断报告增加 AppOps、敏感短信能力、默认短信角色、安装来源和省电状态，并完成诊断页人工检查、APK 签名校验及源码包完整性校验 | 新 APK SHA-256 为 `bb060b19027b072a14c231756bc233c80dfa6344d2d6aa70b6627b6ede1da720`；小米 14 真机短信样本和诊断报告仍待回传 |
| 2026-09-22 | 诊断脚本增加 `RECEIVE_SMS` 显式 AppOps 查询，并在 Android 15 模拟器成功采集脱敏报告 | 源码包已更新；小米 14 真机仍需运行脚本或使用应用内“复制诊断信息” |
| 2026-09-22 | 诊断报告补充系统构建号和安全补丁级别，用于对应 HyperOS 版本 | 17 项 Android 测试、Lint 和 APK 签名校验通过；小米 14 真机报告仍待回传 |
